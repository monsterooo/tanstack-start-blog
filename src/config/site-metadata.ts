import { enUS } from 'date-fns/locale' // 导入对应 locale

export const siteMetadata = {
  stickyNav: false,
  headerTitle: 'TanStack Blog',
  description: 'A blog created with Tanstack Start and Tailwind.css',
  locale: enUS,
}
